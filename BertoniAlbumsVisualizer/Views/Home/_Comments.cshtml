@model List<Models.Comments>
@{
    ViewData["Title"] = "_Comments";
}

<h1>_Comments</h1>

<div>
    @foreach (var item in Model)
    {
        <h2> @item.name </h2>
        <h3> @item.email </h3>
        <h5> @item.body </h5>
    }
</div>